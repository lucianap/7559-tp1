set(SOURCE
        ${SOURCE}
        ${CMAKE_CURRENT_SOURCE_DIR}/FileManager.cpp
        PARENT_SCOPE
        )
set(HEADERS
        ${SOURCE}
        ${CMAKE_CURRENT_SOURCE_DIR}/FileManager.h
        PARENT_SCOPE
        )
